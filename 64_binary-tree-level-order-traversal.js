function levelOrder(root) {
	let bfs = [];
	let queue = [];
	queue.push(root);

	while (queue.length) {
		const currTreeLevel = [];
		const currQueueSize = queue.length;
		for (let i = 0; i < currQueueSize; i++) {}
	}

	return bfs;
}
